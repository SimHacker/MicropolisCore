{
	"name": "@micropolis/engine-wasm",
	"version": "0.0.0",
	"private": true,
	"description": "Micropolis C++ simulation core (Emscripten → WASM). Build via makefile; install copies micropolisengine.{js,wasm,data} and generated .d.ts into apps/micropolis.",
	"scripts": {
		"build": "make install",
		"clean": "make clean"
	}
}
