/**
 * Vertical field of view (degrees) for the stage render path and CPU picking helpers.
 * Slightly below 50° reduces strong wide-angle stretch that can read as short or “squat” on screen.
 */
export const MOO_SHOW_VERTICAL_FOV_DEG = 43;
