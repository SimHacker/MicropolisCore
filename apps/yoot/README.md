# `apps/yoot` — placeholder

Future home for a **deployable web app** shell for the tower-line sim (Emscripten/SvelteKit aligned with MicropolisCore), once engine bindings land.

Naming uses **`yoot`** as the umbrella path (creator-associated brand) instead of historical product titles.
